# Frame notes

- **Viewport:** 1920×1080 Playwright capture (`slowMo` + paced clicks; posters captured in a second pass)
- **Posters:** PNG from first frame of each clip (`posters/*.png`)
- **Hero:** 3-panel strip (setup / question / score) with 400ms crossfade in composition HTML
- **Fixture content:** Production bank loads automatically; no custom seed required for demos
